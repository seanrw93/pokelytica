PORT=4000
GROQ_API_KEY="your-groq-api-key"

DATABASE_URL="postgresql://user:password@localhost:5432/pokelytica?schema=public"

# Must match apps/web's INTERNAL_API_SECRET exactly — shared secret so this
# service only accepts requests forwarded by the Next.js server.
INTERNAL_API_SECRET="generate-a-random-string"
